# Actuator
spring:
  zipkin:
    enabled: true
    baseUrl: http://zipkin:9411 #zipkin container name

management:
  endpoints:
    web:
      exposure:
        include: "*"
  info:
    env:
      enabled: true
  endpoint:
    info:
      enabled: true